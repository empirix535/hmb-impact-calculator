// Loads @tanstack/react-start's module augmentation for route options
// (e.g. the `server.handlers` option on createFileRoute) project-wide,
// so generated server routes typecheck without importing react-start.
import "@tanstack/react-start";

export {};
